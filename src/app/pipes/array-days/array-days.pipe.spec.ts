import { ArrayDaysPipe } from './array-days.pipe';

describe('ArrayDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
